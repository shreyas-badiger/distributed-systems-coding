# Call Back Library

## Write two functions as described as below:

### `record(f1 func)`
This function should record a function in a data strcuture.

### `execute()`
This function should execute all the functions that are recorded so far.